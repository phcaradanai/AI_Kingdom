# Project Status

## Current State

AI Kingdom is implemented through the M24 Competent Manager arc (Phase A grounded delegation, Phase C semantic acceptance review, and Phase B supervised mechanical retry), M24 adaptive reasoning, M24 King-selects-external-agent readiness, and M24 self-growing max_tokens, with Premium UX Waves 1, 2, 3, and 4A-4E complete. The current system is a role-aware Kingdom Control Plane with Mission Control, BUILD-mode risk-gated execution, Grand Vizier council orchestration, provider/model routing and usage traces, project/context binding, external-agent WorkOrders, sandboxed runner jobs and patch review, Living Loop scheduling, opt-in Auto Context Repair, persistent reports/knowledge, Treasury, Audit Log, and Charter/Vision governance.

Local source of truth is PostgreSQL through Prisma. The default seeded login is `king@aikingdom.local` / `password123`.

## Active Work

Premium UX Wave 4E for `/living-agents/:agentId` is complete. No Codex implementation is active; Premium UX Wave 4F for `/providers` is the next reserved candidate and must begin with synchronization, discovery, source-ownership audit, and baseline capture.

## Implemented Milestones

- Capture-new-lessons loop (complete) — makes the learning loop self-sustaining. Previously knowledge candidates came only from council summaries; a failed runner-job review (the clearest signal of what went wrong) produced nothing reusable. Now, when `CAPTURE_LESSONS_FROM_REVIEWS` (**default OFF**) is on, `createOrUpdateAgentReviewForJob` proposes a PENDING knowledge candidate whenever a review verdict is a diagnosed failure (`NEEDS_FIX` / `PATCH_FAILED` / `VALIDATION_FAILED` with non-empty `whatFailed`), attributed to the responsible agent, category `BUG_LEARNING`. Once the King approves it in the Knowledge Lab it flows back into council + planner via `AGENT_KNOWLEDGE_IN_CONTEXT` — closing the full cycle (decree → execute → review → lesson → King approves → smarter reasoning). Best-effort (never blocks the review), King-gated, and routed through the existing M16 value gate (dedup + provenance + linkage). A subtle but important fix surfaced here: the value gate requires a `traceId`, so the capture passes a `review:<jobId>` provenance marker (the deterministic review has no AIUsageTrace) — without it every review lesson would have been silently rejected. 2 tests (failed review → PENDING candidate; gate OFF → none); the 11 existing review tests still pass; root typecheck green. Added to `npm run intelligence:enable`. No migration; no new autonomy; no push/PR/merge/deploy.

- Premium UX Wave 4E Living Agent Evidence Profile refinement (complete): Replaced the 876-line, 12-tab `/living-agents/:agentId` page with a 6-line route and five focused evidence sections: Overview, Timeline, Work & Relationships, Usage & Traces, and Knowledge & Audit. Profile data loads immediately while timeline, relations, candidates, and memories load only when requested; request guards prevent duplicate or stale relation/knowledge updates. Section failures are distinct from empty results, support retry, and retain partial knowledge evidence. Semantic English/Thai chrome, 44px controls, explicit owner links, legacy-attribution disclosure, and reduced-motion-safe status feedback preserve canonical Agent, activity, trace, project, council, report, provider, knowledge, and audit ownership. Every Wave 4E file is below 600 lines. Focused tests pass 8/8, the full web suite passes 219/219, and web lint, root typecheck, root build, and diff checks pass. Responsive browser verification remains outstanding because the available in-app browser tab was locked on an error document; no visual-pass claim is made.

- Premium UX Wave 4D Living Agents Roster refinement (complete): Rebuilt `/living-agents` from eight repeated decorative cards into a compact operational roster and focused evidence pane. Existing Kingdom Presence supplies optional RBAC-safe live task, Work Order, progress, and blocking evidence; denied or unavailable presence degrades to roster evidence without blocking the page. Agent identity/portrait, AgentActivity status and attribution, WorkOrder assignment, provider usage, and profile timeline remain source-owned and directly linked. State/role filters, 44px controls, explicit roster/detail handoff below `xl`, reduced-motion-safe state feedback, and semantic English/Thai chrome now cover the route. The 246-line route is 6 lines and every controller, model, toolbar, roster, evidence, workspace, message, and test file is below 180 lines. Focused tests pass 6/6; the full web suite passes 211/211; root typecheck, web lint, and root build pass. Live EN/TH checks at 1440x900, 1024x768, and 430x932 show aligned desktop panes, one-pane tablet/mobile flow, no horizontal overflow, canonical source links, and no console errors. The existing large-chunk and React Router future-flag warnings remain.

- Curated knowledge in council reasoning (complete) — closes the M16 loop where approved `AgentKnowledgeMemory` was created after councils but never consumed during decree reasoning (`buildAgentKnowledgeContext` was only reachable from a route). When `AGENT_KNOWLEDGE_IN_CONTEXT` (**default OFF**) is on, `grandVizierOrchestrator` now injects **each agent's own APPROVED knowledge** (per-agent + project + global, ranked by use) into that agent's council prompt, and the Grand Vizier's curated knowledge into the synthesis — so the King's vetted lessons finally shape live deliberation. Per-agent (computed inside the council pass), additive (appended to the agent's memory context), reuses the M16-tested `buildAgentKnowledgeContext`; root typecheck green and the 12 knowledge-service tests pass. Added to `npm run intelligence:enable`. No migration; no new autonomy; no push/PR/merge/deploy. The planner now consumes the same curated knowledge too — an `[APPROVED KNOWLEDGE]` section built from the planner agent's approved memories, under the same `AGENT_KNOWLEDGE_IN_CONTEXT` gate — so both the council deliberation and the work-order planning reason with the King's vetted lessons.
- Cross-task learning for the council (complete) — extends the planner's cross-task learning to the council deliberation itself. When `COUNCIL_CROSS_TASK_LEARNING` (**default OFF**) is on, `grandVizierOrchestrator` computes the same relevance-ranked, outcome-gated lessons (`buildCrossTaskLessons`, reused) once per decree and appends them to the shared `kingdomMemoryContext` — which already flows into every specialist's prompt **and** the Grand Vizier's synthesis — so the whole council reasons with the relevant what-worked / what-to-avoid history, not just the post-hoc planner. Single injection point (lines where memory context is assembled), additive, deterministic, no extra provider call. Lesson generation is covered by the existing `crossTaskLearningService` tests; root typecheck green. Added to `npm run intelligence:enable`. No migration; no new autonomy; no push/PR/merge/deploy.
- Cross-task learning for the planner (complete) — the planner planned each decree largely from scratch: it loaded past work orders/reports/briefs by **recency**, never by **relevance**, and never consumed the Kingdom's clearest signal of what actually worked or failed — the per-job review outcomes. New `crossTaskLearningService.ts` turns those outcomes into lessons: `findCrossTaskLessons({decreeText, projectId})` reads `AgentReviewSummary` records, keeps the latest per work order, scores each by keyword relevance to the current decree (title weighted over objective), and classifies a decided review as **WORKED** (PASS) or **AVOID** (a failure verdict with a diagnosis), skipping ambiguous verdicts (RISK_REVIEW/UNKNOWN) and zero-relevance work. The top lessons are formatted into a `[LESSONS FROM SIMILAR PAST WORK]` planner section ("what worked" + "what to avoid") so the planner reuses wins and stops repeating past failures. It is deterministic and provider-free (no extra AI call), outcome-gated to resist pollution, and gated behind the new setting `PLANNER_CROSS_TASK_LEARNING` (**default OFF**); when off, planner behavior is unchanged. No Prisma migration (reuses existing review records). Demonstrated (the falsifiable check): for an OAuth-token decree the service surfaces the relevant prior failure (with its diagnosis) and the relevant prior success, while excluding an irrelevant recent failure (which a recency-based loader would have leaked) and an ambiguous-verdict item. Tests: 2 service tests (relevance + outcome gating; empty-when-nothing); root typecheck green. Safety unchanged: no new autonomy, no push/PR/merge/deploy.
- Premium UX Wave 4C Agent Chat refinement (complete): Rebuilt `/agent-chat` from a 400-line three-column page that widened to 564px on a 430px viewport into a focused agent/session browser, conversation, and context/source workspace. Desktop uses three aligned panes; tablet and mobile show one explicit pane at a time while preserving the message draft. Semantic English/Thai chrome now covers request modes, composer, save ownership, source records, loading, empty, sending, and error states while agent identity, project names, messages, provider/model evidence, and server errors remain source data. Direct-agent APIs, project binding, save modes, Artifact/Knowledge Candidate review ownership, provider routing, RBAC, secrets, and advisory-only boundaries remain unchanged. The route is 7 lines and every new controller, model, rail, panel, workspace, message, and test module is below 205 lines. Focused tests pass 6/6, the full web suite passes 205/205, root typecheck, web lint, and root build pass. Live EN/TH checks at 1440x900, 1024x768, and 430x932 show no horizontal overflow, 44px controls, aligned desktop panes, explicit mobile transitions, and no console errors; only existing React Router future warnings remain.
- Council parallelization (complete) — the council previously ran its specialists strictly one-by-one (a sequential round-table where each agent's prompt carried the prior agents' answers via `previousCouncilContext`). New opt-in setting `COUNCIL_PARALLEL_SPECIALISTS` (**default false**, current behavior) runs the four specialists concurrently instead: the per-agent loop body was extracted into a `runCouncilAgentPass(agent, previousCouncilContext)` closure (default path stays bit-identical), and when enabled the specialists fire through `Promise.allSettled` (one failure no longer sinks the council) with an empty peer transcript — independent opinions — while the Grand Vizier still runs **last** with the full transcript and then synthesizes, so only specialist peer-anchoring is removed. This is a deliberation-model change, not just a speed knob, so it ships behind a King-flipped setting with an A/B rather than flipped by default. Critical correctness fix: council responses are persisted by `createdAt`, which is non-deterministic under concurrency, so the returned/displayed responses (and downstream memory + learning candidate) are now re-sorted by the canonical council order in both modes (`sortByCouncilOrder`; a no-op for the sequential path). No Prisma migration. Live A/B on the same PLAN decree (real OpenRouter providers): sequential ≈ 356s vs parallel ≈ 173s (~51% faster), with identical 5-role canonical order and a non-empty final summary in both. Tests: existing role-council order test still green (sequential), plus a new parallel-council test asserting canonical order is preserved despite concurrent persistence; root typecheck green. Safety boundaries unchanged: no new autonomy, results/flow identical, no push/PR/merge/deploy.

- M22 Phase 2 / UX Wave 4B External Agents Registry refinement (complete): Rebuilt `/external-agents` from repeated registry cards plus an always-visible editor into a compact searchable registry, symmetric status strip, runner-readiness signal, and selected detail with Identity, Capabilities, Handoff, Validation, and Source sections. Readiness reasons remain computed API/runner evidence; configuration tests are explicitly non-executing; Work Orders, Automation Jobs, and Matters remain canonical linked sources. King-only create/edit dialogs, activation controls, and soft-delete confirmation preserve RBAC and manual-handoff boundaries. The route is 10 lines, and controller, models, registry, detail, dialogs, workspace, and semantic message modules are each below 220 lines. Focused tests pass 8/8 and the full web suite passes 199/199; root typecheck and production build pass. Live EN/TH checks at 1440x900, 1024x768, and 430x932 show no horizontal overflow, 44px controls, responsive selected-detail handoff, and background-scroll locking. Browser logs contain only existing React Router v7 future-flag warnings.
- Live Kingdom state-driven motion refinement (complete): Upgraded Throne Room `/throne-room` Live Kingdom from fixed portrait markers to a lightweight moving court inspired by Pixel Agents' observable character-state model, without importing its assets or runtime. A pure deterministic motion model keeps every agent inside role-owned room bounds, separates residents into stable lanes, and maps real `AgentPresenceState` values to working/council/review/attention depth while ambient idle walking remains explicitly non-operational. State changes trigger travel, active states expose compact Lucide activity signals, portraits still come from the canonical display profile, and selected Work Order links now focus the owning record directly. Motion freezes under `prefers-reduced-motion`; semantic English/Thai chrome, 44px mobile controls, and a memoized no-provider i18n fallback prevent render loops. Motion/controller/character/message modules are each below 130 lines. Focused Kingdom tests pass 13/13, the merged full web suite passes 193/193, web and root typecheck pass, and the production build passes with the existing large-chunk warning. Live EN/TH checks at 1440x900 and 430x932 show deterministic position changes over time, no horizontal overflow, and only the existing React Router future-flag warnings.
- M22 Phase 2 / UX Wave 4A Agents Registry refinement (complete): Rebuilt `/agents` from a 1,740-line registry plus always-visible configuration form into a compact searchable registry, symmetric status strip, selected-agent evidence workspace, and explicit create/edit dialog. Identity, Prompt, Skills, Routing, Fallbacks, and Preview are stable sections; canonical `Agent` identity and `Agent.config.displayProfile` presentation ownership remain distinct and directly visible. Links lead to Living Agent, Providers, and Routing ownership instead of copying their data. Existing agent APIs, provider/model routing, row-level fallback validation, 800ms debounce, stale-result guards, non-blocking save refresh, memory safety, secrets, lifecycle, and RBAC remain unchanged. The route is 7 lines; controller, models, registry, detail, editor, routing, fallback, parameters, preview, and semantic message modules are each below 430 lines. Focused tests pass 6/6 and the full web suite passes 185/185; root typecheck and production build pass. Live EN/TH checks at 1440x900, 1024x768, and 430x932 show no horizontal overflow, 44px controls, responsive selected-detail handoff, and background-scroll locking in the editor. Desktop page height dropped from about 3,795px to about 1,160px. The existing large-chunk and React Router future-flag warnings remain.
- M22 Phase 2 / UX Wave 3C Strategy Ledger refinement (complete): Rebuilt `/strategy` from four simultaneous creation forms and scattered record panels into a compact strategic overview with a real next-action signal and stable Overview, Objectives, Opportunities, Assets, and Revenue views. Mutations now use focused create/edit dialogs while preserving the existing Strategy Ledger APIs, Prisma ownership, KING/CROWN_PRINCE write RBAC, research intake, opportunity-to-Work-Order transition, and lifecycle values. Project, Artifact/Report, Usage Trace, and Work Order references remain directly navigable instead of becoming duplicated strategy data. The 577-line route is now 16 lines; controller, models, overview, collections, dialog, workspace, and strategy message modules are each below 560 lines. Semantic English/Thai chrome, 6 focused tests, root typecheck, 182/182 web tests, and root production build pass. Live checks at 1440x900, 1024x768, and 430x932 in English and Thai show no horizontal overflow or console errors; page controls are at least 44px and mobile dialogs lock background scroll. The existing large-chunk warning remains.
- Agent identity and portrait source-of-truth repair (complete): `Agent` columns remain the canonical operational identity and `Agent.config.displayProfile` is the single presentation profile. Added one shared API normalizer and removed duplicated profile parsing from Agents, activity, Living Agents, Treasury, direct chat, and Kingdom Presence. Presence now carries slug/title/display title/avatar URL/version so Throne Room and Operations render the same uploaded portrait and cache version as `/agents`; the scene no longer guesses `/kingdom/sprites/<name>.png`, which previously ignored custom portraits and could reference missing files. Living Kingdom detail links to the owning agent profile. Council synthesis uses the recorded Grand Vizier response agent when present and a neutral synthesis symbol otherwise, never a hardcoded person. Royal Brief now preserves avatar version. Root typecheck, 176/176 web tests, 2/2 pure API profile tests, production build, and live checks on `/agents`, `/throne-room`, `/kingdom/operations`, and `/council` at 1280px and 430px pass without horizontal overflow or console errors. The existing large-chunk warning remains.
- UX terminology regression audit (complete): Replaced the internal `M21` Living Loop eyebrow with semantic English/Thai labels and removed `M17D-1`, `M17D-2`, and `M23` references from user-facing Living Loop and Settings descriptions. Existing setting values, autonomy gates, source records, and milestone-bearing Work Order data remain unchanged. Default-setting bootstrap now refreshes description/category metadata without overwriting operator values. Static chrome and API setting-description regression tests pass; root typecheck, 174/174 web tests, production build, and live EN/TH checks at 1440x900 and 430x932 pass without horizontal overflow. The existing large-chunk warning remains.
- M24 Phase A — Grounded delegation (complete): "Make the kingdom agent understand the work so it can command the external agent and supervise it well." First of an A→C→B sequence (A grounding, C semantic review, B supervised auto-retry). Phase A closes the gap where the council/planner's understanding of a BUILD decree died in provenance and never reached the executor. The planner now emits **decree-specific `acceptanceCriteria`** (the BUILD prompt asks for 2–5 concrete, verifiable conditions; parsed tolerantly; absent ⇒ the previous generic baseline), and `createDraftWorkOrders` stores them on the work order instead of the same four boilerplate lines for every order. `buildExternalAgentPrompt` now sources "Files Likely Involved" from `provenance.executionMetadata.fileHints` (the paths the council identified), falling back to the keyword guess (`inferLikelyFiles`) only when no hints exist. No Prisma migration — `provenance` is already a Json field and `WorkOrder.acceptanceCriteria` already existed; no DTO changes (`PlannerDraft` is internal). Scope intentionally limited to the council→planner→external-agent path the King exercises; `generateWorkOrderFromTask`/`generateWorkOrderFromMatter` keep their baseline criteria. Safety boundaries unchanged: no new autonomy, results still land `NEEDS_REVIEW`, no branch push/PR/merge/deploy. 3 new tests (planner acceptanceCriteria parse + tolerant drop; prompt surfaces provenance fileHints over keyword guess); API typecheck clean; full `npm run test:api` 788/793 with only 5 pre-existing environment-dependent failures (4 planner `COUNCIL_AUTO_WORK_ORDER_MODE` setting/order tests + 1 provider-budget routing test), unchanged from baseline. King's recorded product decision for the deferred Phase B (supervised auto-retry): let the kingdom agent self-fix mechanical failures first, escalating to the King only after retries are exhausted — always inside the existing autonomy line.
- M24 Phase C — Semantic acceptance-criteria review (complete): Builds on Phase A's decree-specific criteria. The AI reviewer was previously told to "preserve the deterministic verdict," so it could only confirm tests-ran / patch-applied — never whether the work satisfied the King's decree; a mechanically-green result that missed the intent still returned PASS / APPROVE. Phase C gives the reviewer semantic-verification authority in the safe direction only: `buildAiReviewPrompt` now includes the work order's `acceptanceCriteria` and asks the model to judge each one (met/unmet) from the diff, files changed, and report; `parseAiReviewJson` reads `acceptanceCriteriaAssessment` where only an explicit `met:false` counts as unmet (ambiguity is treated as met, so uncertainty never penalizes); `mergeAiReview` applies a **constrained downgrade** — when the deterministic verdict is PASS but ≥1 criterion is unmet it flips to NEEDS_FIX / REQUEST_REVISION, folds the unmet criteria into `whatFailed`, adds a revision next-action, and emits a revision prompt. It can only ever make the verdict more conservative; it never upgrades a failure to a pass. The verdict is advisory (no path auto-approves a patch from it), so this purely sharpens the King-facing signal, which the existing `/automation-jobs` review card already surfaces. No Prisma migration (the assessment is in-memory; the downgrade lands in the existing `verdict`/`whatFailed` columns). The semantic check runs on the King's AI review regeneration — the deterministic default path cannot judge semantics, and auto-running the AI review is the deferred Phase B's concern. 3 new tests (unmet → downgrade + revision prompt; all-met → stays PASS; ambiguous → no downgrade); API typecheck clean; full `npm run test:api` 791/796 with the same 5 pre-existing environment-dependent failures, unchanged from baseline.
- M24 reasoning-parameter fix (complete) — makes the decree→work-order chain actually fire on real providers: `buildProviderRequestBody` in `modelParameterResolver.ts` was emitting BOTH `reasoning.effort` and `reasoning.max_tokens` whenever an agent's stored `modelParameters` carried both, which OpenRouter rejects with HTTP 400 ("Only one of reasoning.effort and reasoning.max_tokens can be specified"). That failed every real provider in the chain, so the planner silently degraded to the Local Sandbox Baseline (mock) and returned "no valid drafts" — the reason M24 never fired live even though direct provider calls (which send no `modelParameters`) worked. Fix: the two are mutually exclusive — `effort` takes precedence; `max_tokens` is sent only when no effort is in play; a stored config carrying both can no longer reach the provider. Diagnosed from `aIUsageTraceStep` error rows. Verified live: with the planner routed to OpenRouter `deepseek/deepseek-chat-v3-0324`, `planFromSession` drafted 3 real work orders carrying decree-specific `acceptanceCriteria` + `fileHints` (the full M24 Phase A chain end-to-end). 2 regression tests; full `npm run test:api` 793/798 with the same 5 pre-existing failures. Operational note: free-tier OpenRouter models (`*:free`) time out at 90s, native DeepSeek returns 402 Insufficient Balance, and Gemini returns 404/429 — only OpenRouter-hosted DeepSeek models are currently reliable for the planner.
- M24 Phase B — Supervised auto-retry (complete) — closes the last leg of the "Competent Manager" arc (A grounding → C semantic review → B supervised retry). The reviewer already emitted a verdict for every completed job, but `WorkOrder.autoRetryCount` / `maxAutoRetries` were recorded and never acted on — nothing re-dispatched a failed job. New `supervisedRetryService.ts` adds one shared core (`dispatchRetry`) behind two entry points: a **King-triggered** route `POST /api/automation-jobs/:id/retry` (a Retry button appears on a `NEEDS_REVIEW` job whose review verdict is `PATCH_FAILED`/`VALIDATION_FAILED`), and an **auto** path (`maybeAutoRetry`) fired best-effort from `submitReport`, gated by the new setting `SUPERVISED_AUTO_RETRY_ENABLED` (**default OFF**). The retry threads the reviewer's *specific* feedback into the next prompt — `buildExternalAgentPrompt` now renders a "Prior Attempt — Fix These Before Retrying" section (verdict + `whatFailed` + `failedCommands` + the reviewer's revision prompt) whenever `autoRetryCount > 0`, so the agent revises instead of repeating the same failure blind. The superseded attempt is moved to `CANCELLED` (not `FAILED`, which the Living Loop observes and would double-handle), freeing the active-job dedupe guard before the replacement is created; `autoRetryCount` is incremented *before* re-dispatch so the prompt is framed as a revision; the replacement is auto-approved (the retry is the authorization) so a runner can claim it. Conservative boundaries: mechanical failures only (semantic `NEEDS_FIX` / `RISK_REVIEW` stay with the King), the auto path additionally requires LOW priority and an online runner, both paths are capped at `maxAutoRetries` and context is re-validated (FRESH) before any mutation, results always land `NEEDS_REVIEW`, and nothing is pushed/merged/deployed. When retries are exhausted the King is notified (Notice) and the event is audited (`supervised_retry_dispatched` / `supervised_retry_exhausted`). No Prisma migration (the fields already existed). Tests: 8 supervisedRetry policy/guard tests + 2 retry-prompt-threading tests (externalAgentWorkOrderService); root typecheck green; affected service tests and the 21 AutomationJobsPage web tests pass.
- M24 self-growing max_tokens (complete) — per the King's directive: when an AI call needs more than the agent's current `max_tokens` and it's reasonable, allow it and persist the larger budget so later calls use it (e.g. 8000 → 12000 → 16000). The only concrete "needed more than X" signal is `finish_reason=length` (output truncated), so `finishReason` is now captured from the provider (`openAICompatibleProvider`) and threaded through `generateWithFallback` (with the resolved winner type). New `maxTokensAutoGrowService.maybeGrowAgentMaxTokens()` grows the **per-agent** stored budget one step (×1.5, rounded to 1000) when a **real** provider (never sandbox/mock) truncates, persisting **both knobs in sync** — the `agent.maxTokens` column and the effective `modelParameters.max_tokens` (which wins in MANUAL mode) — and audit-logs each growth. Honest mechanism: a truncated response only proves the need was ≥ the sent budget, so it climbs in steps and converges rather than reading an exact value. Two cost guardrails (the King runs budget limits): a hard ceiling `AI_MAX_TOKENS_CEILING` (default 16000 content tokens — verified within deepseek-v4-flash's accepted output cap, which takes 32000) that growth never exceeds, and a kill-switch `AI_MAX_TOKENS_AUTOGROW` (default true). Persists the **content** budget (not the reserve-inflated value sent to the provider) so the reasoning reserve isn't double-counted on the next call. Wired to planner, council agents + Grand Vizier synthesis, and the reviewer. Live-verified: a real provider returns `finish_reason=length` when truncated, growth persists 8000→12000→16000 and halts at the ceiling. 5 grow-service tests; api typecheck clean; full `npm run test:api` green.
- M24 King-selects-external-agent readiness gate (complete) — per the King's directive: the Kingdom uses many external agents (codex, claude-code, cline, hermes, antigravity, devin, cursor, ...) and some are offline at any moment, so when the Kingdom would otherwise *auto-select* an agent it must instead ask the King to choose among the agents that are ready right now. **Live readiness probe via the runner** (the King's chosen approach): the runner is where every external-agent job executes, so a new `agentCapabilityProbe.ts` resolves each agent type's CLI command and checks whether the binary is present on the runner host, reporting the result in its heartbeat; the API stores it on `AgentRunner.agentCapabilities` (+`capabilitiesUpdatedAt`, migration `20260630000000_m24_external_agent_readiness`, which also adds `CURSOR`/`DEVIN` to `ExternalAgentType`). `externalAgentReadinessService.getExternalAgentReadiness()` combines static config (active + bridge-enabled + command + not manual-only) with the latest online runner's reported capability and the agent's last run, exposing per-agent `ready` + a human reason via `GET /api/external-agents/readiness`. The **gate** (`REQUIRE_KING_EXTERNAL_AGENT_CHOICE`, default true) fires only when the system would auto-select (the King's chosen trigger): the auto-exec path (`maybeAutoExecuteBuildWorkOrder`) pauses and raises an `AWAITING_ROYAL_DECISION` Matter listing the ready agents instead of auto-picking, and the bridge's auto-select branch refuses (clear 409) so no path silently chooses. The King's explicit assignment/dispatch is unaffected. The choice Matter is idempotent per work order and is resolved (closed) when the King assigns an agent (assign route) or any bridge job is created for it — closing the decision loop. Frontend: the External Agents page shows a live runner-online line and a Ready/Offline+reason badge per agent. No autonomy added; results still land NEEDS_REVIEW; no auto push/PR/merge/deploy. Tests: 5 runner probe + 4 readiness/choice/resolve (DB) + 2 web readiness; runner 103/103, web 184/184, full `npm run test:api` green; api/runner/web typecheck clean. Migration deployed to dev + `ai_kingdom_test`.
- M24 adaptive reasoning (complete) — the responsible agent thinks harder on complex work, per the King's directive ("complex code fix / bug analysis in a large system should use reasoning and may expand maxTokens"). Reasoning is OFF by default on every agent (for v4 stability), so this adds a complexity-aware escalation rather than a blanket enable. New `complexityAssessor.ts` is the single deterministic (no extra LLM call) decision point: `assessDecreeComplexity` scores the decree text + task mode with Thai+English keyword/length signals for the **pre-work-order** stage (planner, council synthesis); `assessExecutionComplexity` uses **structured** signals (risk/priority level, deterministic review verdict, acceptance-criteria count) for the **post-work-order** stage (reviewer, external coder). `resolveEffectiveParameters` gains an optional `ParameterEscalation` argument that forces reasoning ON at high effort when the work is COMPLEX — and is a strict no-op when reasoning is off or the provider doesn't support reasoning (sandbox, native non-OpenRouter), so it is always safe to pass. Wired surgically to the **responsible** agent only, not blanket across the council: the planner (deeper plan for a complex BUILD decree), the Grand Vizier's final synthesis (NOT all 5 council agents — contains cost/instability), and the AI reviewer (bug analysis of a failed or high-risk patch). For the external coder (Claude Code/Codex CLI, per the King's "include the coder too"), `buildExternalAgentPrompt` injects an extended-thinking directive ("think hard"/"ultrathink") into the prompt when the work order is COMPLEX (priority, retry-after-failure, or many criteria) — a prompt-level lever needing no runner env change. Gated by a kill-switch setting `ADAPTIVE_REASONING_ENABLED` (default `true`); every escalation decision + matched signals are recorded in the AI usage trace metadata. Also fixed two latent issues found while verifying: (1) `completeAIUsageTrace` now **merges** completion metadata into the creation-time metadata instead of overwriting it, so diagnostic fields (`complexityLevel`, `reasoningEscalated`, `agentSlug`) survive to the King-facing trace — a fix benefiting every trace; (2) all agents' stored `modelParameters.max_tokens` (800–1800) silently overrode the `agent.maxTokens=8000` the King had set in MANUAL mode and were truncating complex plans — consolidated to 8000 (DB config). No Prisma migration. 12 new tests (8 complexityAssessor + 4 resolver escalation incl. sandbox no-op and request-body shape); full `npm run test:api` green. Verified live end-to-end: a complex BUILD decree escalates the planner to `reasoning {exclude,effort:high}`, runs on `openrouter/deepseek-v4-flash` with **no fallback to sandbox** (`fallbackNotice=null`), `finish_reason=stop` (v4-flash accepts the escalated ceiling `8000+3072=11072`), and produced a real decree-specific work order; an A/B on the same session showed reasoning ON does not regress the planner versus OFF. Safety boundaries unchanged: no new autonomy, no auto branch push/PR/merge/deploy.
- M22 Phase 2 / UX Wave 3B Project Inbox and Artifacts refinement (complete): Rebuilt `/project-inbox` as a compact routing queue with stable search/status/confidence filters, secondary quality/source/project controls, selected routing evidence, deterministic reasons, confidence bands, safe single/bulk assignment, and direct links to the source record, suggested/assigned Project, and usage trace. Rebuilt `/artifacts` as a searchable archive plus read-focused evidence pane with bounded content, project/source/trace ownership, provenance field names without raw values, duplicate review, explicit create/edit dialogs, and King-only destructive confirmation. Existing deterministic routing, thresholds, assignment APIs, artifact contracts, safe-path rules, and RBAC remain unchanged. Route files are 40 and 27 lines; every new controller, model, queue/archive, detail, toolbar, and dialog module is below 150 lines. Semantic English/Thai chrome, 8 focused page tests, web typecheck, 173/173 web tests, and production build pass. Live checks at 1440x900, 1024x768, and 430x932 in English and Thai show no horizontal overflow or console errors; mobile artifact dialogs lock background scroll. The existing large-chunk warning remains.
- M22 Phase 2 / UX Wave 3A Projects refinement (complete): Rebuilt `/projects` as a compact portfolio list with a real context-health and active-work summary, stable search/status/priority filters, a selected-project source rail, explicit create/edit dialogs, and direct links to project context, Work Orders, Inbox, and Artifacts. Rebuilt `/projects/:id` as the owning project workspace with a stable health panel, metric strip, six-section index, bounded Work/Local Docs/Repository evidence, Artifacts ownership, and explicit Obsidian export. Existing project mutation, context repair, safe-path, local-doc root, repository scan, artifact, export, and RBAC contracts remain unchanged. Split page orchestration from controllers, presentation components, and pure helpers: the route files are 64 and 68 lines, and every new project module is below 220 lines. Added semantic English/Thai chrome and focused coverage for explicit mutation, portfolio hierarchy, section navigation, context ownership, source links, local-doc safety, and raw-root-path non-disclosure. Web typecheck, 165/165 web tests (Node 25 requires `NODE_OPTIONS=--no-experimental-webstorage`), and production build pass; live checks at 1440x900, 1024x768, and 430x932 show no page overflow or console errors, and the existing large-chunk warning remains.
- M22 Phase 2 / UX Wave 2D Reports and Decree Lineage refinement (complete): Rebuilt `/reports` as a searchable/filterable archive rail plus selected reading pane and compact provenance rail. Editing remains an explicit secondary mode and deletion now requires a destructive confirmation modal. Rebuilt `/decree-lineage/:workOrderId` as a read-only seven-stage evidence workspace with a compact journey summary, present/missing states, bounded prompt/result disclosures, an evidence index, and direct links to Work Orders, Council, Automation Jobs, Knowledge Lab, Reports, and Royal Command where the current contracts permit. Added semantic English/Thai chrome while preserving server prose, identifiers, raw enums, API behavior, source ownership, lifecycle rules, and approval boundaries. Web typecheck, 161/161 web tests, and production build pass; live checks at 1440x900, 1024x768, and 430x932 show no horizontal overflow, translated clipping, console errors, or sub-44px page controls after the final touch-target pass. The existing large-chunk warning remains.
- Premium UX shell refinement (complete): Desktop navigation now collapses from 256px to an accessible 80px icon rail and persists the browser preference without changing route or RBAC visibility. The shell polls the real next-action count every 30 seconds, uses reduced-motion-safe interaction feedback, retains the mobile drawer, and prevents wide page content from pushing mobile navigation off-screen. Dashboard cards now contain long provenance labels without horizontal page overflow. The UX plan records explicit symmetry, density, real-state animation, and responsive-space rules. Web typecheck, 156/156 web tests, web build, and live 1440x900/430x932 checks pass; the existing large-chunk warning remains.
- Decree Lineage (complete): Added read-only `GET /api/decree-lineage` aggregation and `/decree-lineage/:workOrderId` detail route. The page presents an ordered King-facing trace from decree through council, work ownership, external prompt/result, review/knowledge, and Royal Secretary summary. It is a computed evidence view only; Task, CouncilSession, WorkOrder, AutomationJob/PatchArtifact, AgentReviewSummary, and knowledge/report records remain the owning sources.
- M23 Phases B-C2 (complete): BUILD councils now produce execution-ready role contracts and planner metadata (`riskLevel`, `fileHints`). With `COUNCIL_AUTO_EXECUTE_LOW_RISK` and the external-agent bridge explicitly enabled, only LOW-risk BUILD work with fresh context, safe path hints, no active job, and an online runner may auto-dispatch through the existing bridge. All other risk/context/readiness cases pause with a traced reason. Results still land `NEEDS_REVIEW`; branch push, PR creation, merge, and deploy are never automatic. Runner patch generation now captures created, modified, and deleted files while excluding runner-internal `.kingdom` data.
- M22 Phase 2 / UX Wave 2C Automation Jobs refinement (complete): Reframed `/automation-jobs` as an execution queue and focused review workspace without changing APIs, runner policy, RBAC, lifecycle transitions, or approval gates. Replaced detached metric cards with a compact status strip; added stable review/active/failed/history filters, secondary runner-health disclosure, priority-sorted queue rows, responsive mobile detail handoff, Work Order/project/Living Loop source links, next-safe-action guidance, and section navigation. All mutating execution and patch actions remain detail-scoped and the King approval boundary explicitly states that patch approval, branch push, PR creation, merge, and deploy are never automatic. Added semantic English/Thai chrome and focused hierarchy/source-link coverage. Root typecheck, 155/155 web tests, and web production build pass; live checks cover desktop, mobile, and 200% zoom without observed overlap or horizontal clipping.
- M22 Phase 2 / UX Wave 2B Work Orders refinement (complete): Reframed `/work-orders` as a responsive queue-and-detail workspace without changing APIs, RBAC, context gates, lifecycle transitions, or execution boundaries. Quick status filters stay visible while advanced filters and source generation use disclosures; creation is now an explicit header action rather than an automatic empty-state form. Selected records retain status, context freshness, next safe action, source links, and a compact section index above detailed fields. Added semantic English/Thai chrome and focused coverage for explicit creation, collapsed source fields, section navigation, and Thai layout. Root typecheck, 153/153 web tests, and the web production build pass; live checks cover desktop, 430px mobile, and 200% zoom without observed overlap or horizontal clipping.
- M22 Phase 2 / UX Wave 1 semantic i18n completion (complete): Migrated Kingdom Operations, Royal Brief, Living Loop, Kingdom Health, and Activity Feed static chrome to explicit semantic English/Thai keys. Operational enum labels are translated while raw values remain available in tooltips; server-provided titles, summaries, reasons, names, identifiers, and generated prose remain source data. Added key-parity/interpolation coverage plus Thai page tests that preserve source routes and raw enum evidence. Added an app-wide `prefers-reduced-motion` fallback. Root typecheck, 143/143 web tests, and web production build pass; rendered checks cover the three completed pages at desktop, tablet, and mobile widths in English/Thai without observed overlap or clipping.
- M22 Phase 2 / UX Wave 1B visual refinement (complete): Refined the remaining Mission Control pages without changing APIs, RBAC, lifecycle behavior, or autonomy boundaries. Kingdom Operations now uses equal-height wide-screen zones with internal scrolling and lower-noise agent rows. Royal Brief now reads as a dated executive document with a dedicated decision rail and direct links to the owning operational sources; the rail moves ahead of the long document on mobile. Living Loop now follows Status/Safety, Automation Stages, Candidate Queue, and Run History hierarchy; settings and run details use disclosures, candidate actions wrap safely on mobile, and the single Run Once command lives in the page header. Added semantic layout assertions and `MemoryRouter` coverage for new source links. Its original validation baseline was root typecheck, 138/138 web tests, and web production build; semantic i18n was completed in the subsequent Wave 1 completion step above.
- M22 Phase 2 / UX Wave 1A (complete): Premium application shell and Mission Control visual foundation. Replaced the mobile all-route pill strip with an accessible full-height navigation drawer; desktop navigation now expands by domain and keeps the current domain open. Added route-aware compact/standard/wide content widths, restrained graphite/brass visual tokens, consistent 8px panel geometry, operational typography, responsive page-header actions, and the unframed `PageSection` primitive. Dashboard and Inbox now use a clearer top-action hierarchy, reduced card nesting, stable metric surfaces, and sentence-case labels while retaining all source/provenance links, Inbox context refresh, RBAC, routes, and lifecycle behavior. Added AppLayout interaction coverage for domain expansion and mobile drawer state; updated Living Loop metric tests to use a semantic value marker instead of CSS implementation classes. Rendered verification covered Dashboard/Inbox in English and Thai at desktop and mobile widths; Thai labels fit without overlap or clipping. No backend, Prisma, runner, autonomy, or route changes.
- M22 Phase 1 (complete): Mission Control i18n Foundation — semantic translation keys. Replaced the fragile whole-string DOM-observer translation on the two highest-traffic Mission Control pages (`/dashboard`, `/inbox`) with explicit semantic keys, without removing the legacy translator (other pages still use it). New `apps/web/src/lib/i18nMessages.ts` holds namespaced `en`/`th` keys (`dashboard.*`, `inbox.*`, plus shared `risk.*`, `state.*`, `severity.*`, `entity.*`, `provenance.*`); each `en` value equals the literal that was rendered before migration so existing English assertions keep passing. `apps/web/src/lib/i18n.tsx` gains an **additive** `tk(key, vars)` (with `{var}` interpolation, `en` fallback, then raw-key fallback) on both the provider context value and the no-provider `useI18n` fallback, plus a `useTk()` hook; `translateText` and the MutationObserver are untouched. `DashboardPage.tsx` and `InboxPage.tsx` now render all static chrome (page titles, section/metric labels, actions, empty/error states, filters, source-reference cards, risk-group titles, relative age) via `tk()`. Enum badges render a translated label while keeping the **raw enum in the `title` tooltip** — `InboxPage`'s local Risk/State/Entity badges already had `title="Risk: CRITICAL"` etc.; `StatusBadge` gained optional `label`/`title` props so Dashboard work/job cards pass the raw `status` as the tooltip. `ProvenanceLinks` row labels (Source/Generated By/Related/Updated) are keyed via `useTk()` (hook placed before its early return). Server-provided prose (a record's `title`/`detail`/`nextAction`/`summary`/`why`/`actionLabel`, agent names, `displayState`) is intentionally NOT keyed — it is data; the covered-vs-deferred boundary (including `KingdomHealthStrip`/`KingdomActivityFeed`, shared `timeAgo`/`formatDate`, the non-rendered `LivingLoopDashboardCard`, and the pre-existing `ProvenanceLinks` raw `EntityType #id` link text) is recorded in `docs/MISSION_CONTROL_I18N_KEYS.md` as the per-page inventory for incremental future migration. No route renames, no backend/Prisma/lifecycle/runner/autonomy changes, no duplicate Mission Control data store; Mission Control stays read-only (the WorkOrder context-refresh remains the only Inbox mutation). 4 new web tests (136 total): InboxPage English + Thai chrome/risk-label + source-link + raw-enum-tooltip, ProvenanceLinks Thai row labels, DashboardPage Thai chrome + raw-enum tooltip + preserved source link (`localStorage` reset in `beforeEach`/`afterEach`; Thai set via `LANGUAGE_STORAGE_KEY`). Verified live (preview, KING login) on both pages: EN→TH→EN toggle renders from keys with no double-translation from the observer and no Thai chrome clipping.
- M21 (complete): Auto Context Repair — Living Loop stage (Priority 6: repair stale/missing context without King intervention). New opt-in setting `LIVING_LOOP_AUTO_CONTEXT_REPAIR` (default `false`). New `autoRepairContext(loopRunId, candidates, userId)` in `livingLoopService.ts` acts on the loop's own `WORK_ORDER_REVIEW` candidates whose `provenance.reason` is `context_missing` / `context_stale` / `local_docs_changed_since_binding`: it scans the project's active local-doc roots **once per tick** (read-only) and rebinds each affected WorkOrder to the freshest snapshot via `repairWorkOrderContext` — the exact safe action the King's manual "Refresh Context" button performs. Wired into `runLivingLoopOnce` **before** the validation/patch stages so a repaired (now FRESH) WorkOrder becomes patch-eligible on a subsequent tick. Throttled by `LIVING_LOOP_MAX_DAILY_CONTEXT_REPAIRS` (default 20) and a per-WorkOrder cooldown `LIVING_LOOP_CONTEXT_REPAIR_COOLDOWN_MINUTES` (default 30) keyed on the `living_loop_auto_context_repair` **audit entry** — deliberately NOT on `contextBoundAt`, which `bindFreshContextToWorkOrder` bumps on every bind (including the best-effort MISSING bind at WO creation) and would otherwise skip exactly the WorkOrders that need repair. Each repair marks its candidate `APPLIED` (so the King's inbox no longer nags) and writes an audit entry; skips are audited too (`living_loop_auto_context_repair_skipped`, `context_repair_cooldown_blocked`, `context_repair_daily_limit_blocked`). Status surfaced via `getLivingLoopStatus().autoContextRepair` (`enabled`, `dailyCount`, `dailyLimit`, `cooldownMinutes`, `repairedLastRun`) and displayed read-only on both `/living-loop` and the Dashboard Living Loop card; Dashboard metrics link back to `/living-loop` as the owning source. **Safety boundary, honestly stated:** unlike the M19 scheduler (which added no capability), this *does* remove a manual gate — it satisfies the FRESH-context precondition that previously sat in front of auto-patch. It still never pushes a branch, opens a PR, merges, or deploys, and auto-patches still land `NEEDS_REVIEW`. Because it changes the existing autonomy path's behavior, it is **opt-in and intentionally excluded from the default `npm run autonomy:enable` chain** — enable it explicitly with `npm run autonomy:enable -- --with-context-repair` (or set the setting). No Prisma migration (settings are key-value; uses the existing AuditLog). 5 DB integration tests (`livingLoopAutoContextRepair.test.ts`): disabled→no-op, enabled MISSING→FRESH + candidate APPLIED + audit, no-project skip, cooldown skip, daily-limit skip. Web coverage verifies Living Loop status/limits/safety messaging and Dashboard source links.
- M20 Phase 1 (complete): Mission Control Consolidation — navigation + provenance consistency. Frontend-only consolidation grounded in `docs/KINGDOM_INFORMATION_ARCHITECTURE_AUDIT.md`. The five overlapping read-only summary surfaces the audit flagged — `/dashboard`, `/inbox`, `/kingdom/operations`, `/royal-brief`, `/living-loop` — are collapsed into a single **Mission Control** nav group in `apps/web/src/components/layout/AppLayout.tsx` (items: Overview → `/dashboard`, Action Queue → `/inbox`, Operations → `/kingdom/operations`, Royal Brief → `/royal-brief`, Living Loop → `/living-loop`). This also **surfaces `/royal-brief`**, which existed as a route but was unreachable from the sidebar. Remaining routes regrouped into owning groups: new **Command** group (Throne Room, Council, Strategy Ledger); **Agents & Models** (renamed from Agents; Agents, External Agents, Providers, Routing); Work / Knowledge / System retained. Dashboard, Inbox, agent presence, current operations, and activity cards now answer "Why am I seeing this?" and render reusable source/actor/time provenance. WorkOrder source links focus the owning record via `/work-orders?focus=<id>`; other entities stay on their canonical owning pages. Touched enum badges now use semantic labels with raw values retained in tooltips, and long presence labels wrap within bounded pills. **No route renames** (deferred to Phase 2), no new lifecycle semantics, no Prisma changes, no new data store.
- external-agent-bridge (complete): Throne Room "Execute with external agent" end-to-end bridge. `externalAgentBridgeService.ts` runs Claude Code headlessly for a decree, routes code work to the Royal Architect as steward for review, and reports the outcome back to the King as a Kingdom Memory (LESSON). Env-auth/validation/error-handling fixes applied; see `apps/api/src/services/externalAgentBridgeService.ts`.
- M19 (complete): Autonomous Kingdom Loop — background scheduler. `kingdomSchedulerService.ts` is an in-process worker that drives the existing Living Loop on a timer, closing the long-standing "no background workers" gap that meant the loop only ran on a manual `POST /api/living-loop/run`. `startKingdomScheduler()` is invoked from `server.ts` on listen (never from `app.ts`, keeping the test app timer-free) with an unref'd `setInterval` at `LIVING_LOOP_INTERVAL_MS` (env, default 300000ms, floored at 15000ms). Each tick re-reads `LIVING_LOOP_ENABLED` from settings and only then calls `runLivingLoopOnce("SCHEDULED")`; an in-process flag guards against overlapping ticks and a tick never throws (errors captured into status). Heavy deps (settings + living loop, hence Prisma) are lazy-imported so unit tests inject their own deps with zero DB coupling. New endpoint `GET /api/living-loop/scheduler` (KING/CROWN_PRINCE) exposes `getSchedulerStatus()` (running, intervalMs, enabledSetting, tick/run/skip/error counters, last tick/run/error). Graceful shutdown clears the timer on SIGTERM/SIGINT. Operator tooling: `npm run autonomy:enable` / `autonomy:disable` (`src/scripts/enable-autonomy.ts`) flips `LIVING_LOOP_ENABLED`, `LIVING_LOOP_AUTO_CREATE_VALIDATION_JOBS`, `COUNCIL_AUTO_WORK_ORDER_MODE` (default READY, `--mode=DRAFT` option), and opt-in `LIVING_LOOP_AUTO_SANDBOX_PATCH` (`--with-sandbox-patch`). Safety boundary unchanged: the scheduler adds no new capability; all data-value, risk-policy, runner-refusal, and NEEDS_REVIEW gates remain; no auto branch push, PR, merge, or deploy. Also: `.env` `AI_PROVIDER` set to `openrouter` to make explicit that, with real keys present, the council already routes to a real LLM (OpenRouter primary → DeepSeek → sandbox fallback) rather than mock. 6 pure unit tests (no DB) for the tick logic: skip-disabled, run-enabled, overlap guard, error capture (no throw), in-progress flag cleared after error, interval flooring.
- M1: Initial React/Vite web foundation and Express/Prisma API foundation.
- M2: Throne Room task intake and task persistence.
- M3: Grand Vizier orchestrator with deterministic council sessions and agent responses.
- M4: AI provider abstraction with mock and OpenAI-compatible providers, fallback handling, model/token settings, and provider metadata.
- M5: Kingdom Memory v1 with CRUD, search, deterministic auto-extraction, duplicate checks, and memory context injection.
- M6: Royal Reports and Council Archive polish with generated reports, search/filtering, source links, and council detail views.
- M7: Agent configuration and system settings with editable prompts, skills, priority, active state, and orchestration settings.
- M8: Staging deployment preparation with `docker-compose.staging.yml`, Dockerfiles, safe Prisma deploy flow, health checks, and backup/restore scripts.
- M9: Auth hardening with bcrypt login, JWT access tokens, refresh token sessions, logout revocation, RBAC, King-only user management, profile/security pages, and audit logging.
- M10: Royal Treasury with per-call token usage tracking, configurable pricing table, cost aggregation by agent/provider/model, daily spend chart, budget limit settings with warning banner, and a `/treasury` dashboard page (KING-only).
- M11: Audit Log UI + Operational Oversight. Read API for `AuditLog` records with filters (action, resourceType, userId, date range), pagination, and full-text search. Sanitization layer scrubs sensitive metadata keys (password, token, apiKey, secret, credential) recursively before any response. `/audit` KING-only dashboard with search, filters, paginated table, and detail panel. `/security` page enhanced with Kingdom Operational Status (live API and DB health checks, token expiry display, session status).
- M11.5: Kingdom Charter Foundation. `KingdomCharter` + `KingdomVision` Prisma models + migration. Seed loads `docs/KINGDOM_CHARTER.md` and `docs/KINGDOM_VISION.md` idempotently. `charterService.ts` provides get/update/seed/format functions. `kingdomComplianceService.ts` loads kingdom context before every council session, auto-seeds from files if DB records are missing, and never throws. Context injected into every agent call (both specialist loop and Grand Vizier synthesis pass) via `GenerateAgentResponseInput.kingdomContext`. API: `GET /api/charter`, `GET /api/vision` (all authenticated); `PATCH /api/charter`, `PATCH /api/vision` (KING only). Frontend: `/charter` and `/vision` pages with section rendering and KING edit mode, visible to all authenticated roles.
- M12: Royal Secretary Core. `Notice` (INFO/WARNING/CRITICAL severity, UNREAD/READ/ARCHIVED status) and `Matter` (8-state status, 4-priority, 7-category) Prisma models + migration. `royalSecretaryService.ts` with `createNotice` (24h title+severity dedup), `createMatter` (sourceType+sourceId dedup in non-terminal states), `inspectKingdomStatus`, `generateDailyBrief`. `GET /api/secretary/brief` (all authenticated). Full CRUD for `/api/notices` and `/api/matters` (read: all roles; create/delete: KING; update: KING+CROWN_PRINCE). Dashboard updated with Kingdom Status pills, Recommended Actions list, Urgent Notices panel, Awaiting Decision panel, and Prime Directive reminder. `/notices` and `/matters` frontend pages with filters, pagination, inline mark-read/archive, create form (KING), and detail panel. `KingdomCharter` and `KingdomVision` Prisma models migrated. Seed service loads `docs/KINGDOM_CHARTER.md` and `docs/KINGDOM_VISION.md` idempotently (never overwrites existing records). `charterService.ts` provides get/update/seed/format functions. `kingdomComplianceService.ts` loads kingdom context before every council session, auto-seeds from files if DB records are missing, and never throws. Context injected into every agent call (both specialist loop and Grand Vizier synthesis pass) via `GenerateAgentResponseInput.kingdomContext`. API: `GET /api/charter`, `GET /api/vision` (all authenticated); `PATCH /api/charter`, `PATCH /api/vision` (KING only). Frontend: `/charter` and `/vision` pages with section rendering and KING edit mode, visible to all authenticated roles.
- M12.5: AI Provider Registry + Routing Policy. Added `AIProvider` and `AIProviderRoute` Prisma models, env-first provider registry, reusable OpenAI-compatible provider client, runtime support for mock/OpenAI/OpenRouter/DeepSeek/generic compatible providers, future metadata stubs for Anthropic/Gemini/local, agent-level provider/model/fallback/cost overrides, `AI_COST_MODE`, chained provider fallback, providerId usage tracking, `/api/providers` KING-only admin API, `/providers` admin page, and provider status in `/settings`. API keys remain server-only and are not returned by settings/providers APIs.
- A1: Provider Registry UI Polish + Add Provider Flow. Overhauled the `/providers` UI to feature labeled fields, dynamic readiness badges, inline edit mode, and an "Add Provider" modal. Extended the backend to support `POST` and `DELETE` on `/api/providers`. Protected custom providers by strictly requiring that their secret keys be referenced only via environment variable names (e.g. `CUSTOM_API_KEY`) to prevent literal secrets from being transmitted or displayed.
- M13: External Agent Work Order + Handoff System. Added `ExternalAgent`, `WorkOrder`, `WorkSession`, `ImplementationReport`, and `HandoffBrief` Prisma models with manual copy-paste execution mode. Seeded Claude Code, Codex, Cline, Kilo, Antigravity, and Hermes as external executor targets. Added `externalAgentWorkOrderService.ts` for task/matter work-order generation, context-drift-resistant prompts, implementation reports, handoff briefs, decision memory capture, and completion report summaries. API: `/api/external-agents`, `/api/work-orders`, `/api/work-sessions`, `/api/implementation-reports`, `/api/handoff-briefs`. Frontend: `/external-agents`, `/work-orders`, dashboard External Work summary. M13 does not call external agent APIs or execute shell commands from the backend.
- M14: Project Workspace + Royal Secretary Project Routing. Added `Project`, `ProjectRoutingCandidate`, `ProjectInboxItem`, and `Artifact` Prisma models plus optional `projectId` links on tasks, matters, notices, council sessions, reports, memories, work orders, implementation reports, and handoff briefs. Seeded AI Kingdom, Godot Tower Defense, Admin Dashboard Boilerplate, E-commerce Inventory Boilerplate, and Backend Go Services idempotently. Added `projectRoutingService.ts` for explainable keyword/alias/name/source-ancestry routing: confidence >=80 auto-assigns, 50-79 creates a suggested inbox item, and <50 creates a pending inbox item without assignment. Added compact project context injection for council processing and external-agent prompts. Added `/projects`, `/project-inbox`, and `/artifacts` pages plus Obsidian markdown export payloads.
- M17E-1 (complete): Local Project Documents + Repository Intelligence. `LocalDocumentRoot`/`LocalDocumentSnapshot`/`LocalDocumentInsight` models; `safePathService.ts` resolver (no traversal, no symlink escape, secrets/env/node_modules/build output blocked, allowlist globs); scanner with provenance (rootId, root path hash, relative path, size, mtime, scan time), stack/scripts/risk-zone detection, and staleness window; project local-docs API under `/api/projects/:id/local-docs` (roots/scans: KING+CROWN_PRINCE, guarded read-file: KING only, all reads audited); agent context injection; Living Loop local-docs health checks + candidates; Royal Brief `localDocsSummary`; runner fresh-local-context policy.
- M17E-2 (in progress → this milestone): Repository Snapshot + WorkOrder Context Binding. `projectContextBindingService.ts` binds WorkOrders/AutomationJobs/PatchArtifacts/ImplementationReports to exact local-doc + repository snapshot ids with FRESH/STALE/MISSING/PARTIAL status, summary, and hash-only provenance. SANDBOX_PATCH requires project linkage + FRESH context (API 409, Living Loop `ContextBinding:*` skip reasons, runner refusal); VALIDATION_ONLY proceeds with warnings. New routes: work-order context get/bind/mark-stale + project context-health. UI: WorkOrder Project Context panel, job context badges, patch "Base Context Used", Living Loop context skips, Royal Brief Context Health.
- M17E-2 hotfix: Manual acceptance was blocked when the dev `AgentRunner` table was empty, because runner auth hashes `RUNNER_TOKEN` and matches `AgentRunner.tokenHash`. Added `npm run runner:bootstrap` to load root `.env`, require `RUNNER_TOKEN`, store only the sha256 hash, and create/update the `Local Runner` runtime row; `db:seed` also bootstraps it when `RUNNER_TOKEN` is present and warns without failing when missing.
- M17G.5 (complete): Imported Patch Status Hardening. Replaced free-form `importedPatchStatus` strings with a validated enum (`PENDING`, `CHECK_FAILED`, `APPLIED_IN_SANDBOX`, `VALIDATED`, `VALIDATION_FAILED`, `NO_CHANGES`) defined consistently across API, runner, and web types. The runner status route (`PATCH /api/runner/jobs/:id/status`) now validates `importedPatchStatus` against this enum and rejects unknown values. Fixed VALIDATED semantics: the runner now sets `VALIDATED` only when validation commands all pass, `VALIDATION_FAILED` when any fail, and `NO_CHANGES` when the workspace diff is empty after patch apply — these were previously collapsed into a single `VALIDATED` state. Fixed import race condition: patch import is now restricted to `QUEUED` jobs only; importing on `APPROVED`, `RUNNING`, `CLAIMED`, `NEEDS_REVIEW`, `COMPLETED`, `FAILED`, or `CANCELLED` jobs returns HTTP 409 Conflict. Improved API response codes: 404 for missing job, 409 for invalid job status, 422 for blocked/unsafe patch content, 400 for malformed patch. Web UI: Import Patch button now only appears for `QUEUED` jobs (not `APPROVED`), with a helper note reminding the King to import before approval; badge rendering handles all six statuses explicitly including the new `VALIDATION_FAILED` and `NO_CHANGES` entries. Branch push and PR creation remain disabled.
- FOUNDATION_OPERATIONS_CENTER (complete): Kingdom Operations Center — Phase 1–4 STAR_OFFICE_UI foundation. Three new backend services: `kingdomPresenceService.ts` (aggregates agent presence state from `AgentActivity` + `AutomationJob` records into per-agent state machine: IDLE/THINKING/COUNCIL/WORKING/RUNNING/WAITING_REVIEW/BLOCKED/ERROR with explicit precedence; `currentTask`, `currentWorkOrder`, `progress` derived from real run-step counts); `kingdomActivityService.ts` (unified activity stream from CouncilSession, WorkOrder, AutomationJob, AgentRunStep, AgentReviewSummary, AgentKnowledgeCandidate — last 48h, limit param, sorted descending); `kingdomHealthService.ts` (6 health dimensions: Context Health, Review Queue, Runner Queue, Provider Availability, External Agent Backlog, Knowledge Processing — each HEALTHY/WARNING/CRITICAL with reason and recommendedAction; `overallStatus` = worst item). API: `GET /api/kingdom/presence`, `GET /api/kingdom/activity?limit=N`, `GET /api/kingdom/health` (all KING/CROWN_PRINCE). DTOs added to both `apps/api/src/types/api.ts` and `apps/web/src/types/api.ts`; client methods in `apps/web/src/lib/api.ts`. Frontend: `KingdomOperationsPage.tsx` at `/kingdom/operations` — top health strip with color-coded clickable dimension pills; 3-column desktop layout (Agent Presence | Current Operations | Activity Stream); agent state dot + pill with precedence-matched color; current task/work order links; progress label; blocking reason; activity feed with type badge, actor, summary, time-ago; current ops summary counts (Running/Thinking/Awaiting Review/Blocked) + needs-review action list + recent job feed. Route registered in `main.tsx`; "Operations Center" nav item (MonitorPlay icon) added to Command section for KING/CROWN_PRINCE. No Prisma schema changes. No breaking API changes. No Dashboard modifications. 9 API tests (3 per service: presence IDLE, WAITING_REVIEW, BLOCKED; activity shape + recent WO entry + limit; health shape + context WARNING + overallStatus invariant); 3 web tests (3-column render, state pill for RUNNING, refresh re-calls all 3 APIs). No mock data; all values derived from existing Kingdom entities.
- M18C-4 (complete): Auto Context Refresh Pipeline. Single safe action (`POST /api/work-orders/:id/refresh-context`, KING/CROWN_PRINCE) that scans all active local document roots for the work order's project, then rebinds context — eliminating the manual "run local docs scan, then rebind" two-step. `refreshWorkOrderContextService.ts` implements `refreshWorkOrderContext(workOrderId, userId)`: loads the WO; if no project → `{ status: "SKIPPED", skipReason: "no_project" }`; else iterates `listLocalDocumentRoots` and calls `scanLocalDocumentRoot(root.id)` per active root (scan failures recorded in `scanFailures[]`, not thrown); then calls `bindFreshContextToWorkOrder` and returns `{ status, oldStatus, newStatus, scanRan, scanFailures, warnings }`. `RefreshWorkOrderContextResult` type added to both `apps/api/src/types/api.ts` and `apps/web/src/types/api.ts`. `api.refreshWorkOrderContext(id)` added to `apps/web/src/lib/api.ts`. InboxPage: "Bind Context" next-action items (discriminated by `item.id.startsWith("WorkOrder:ctx:")`) now render as an inline action button — `handleContextRefresh` calls `api.refreshWorkOrderContext(item.entityId)` and refetches the queue; shows "Refreshing…" spinner during the call. All other items remain as links to `routeTo`. WorkOrdersPage: removed the confusing `repairContext()` + "Repairable: YES/NO" logic; the stale-context warning panel now shows a single "Refresh Context" button (calling `runRefreshContext()` → `api.refreshWorkOrderContext`) that handles both scan and rebind automatically; the top "Refresh Context" panel button also calls `runRefreshContext()`. No external agents, no automation jobs, no patches, no push/PR/merge/deploy. No new Prisma migration. 5 API service tests: no-project SKIPPED, MISSING→scans→FRESH, STALE-with-changed-docs→scans→FRESH, scan-failure records scanFailures and still rebinds, no-active-roots→PARTIAL. 3 new InboxPage web tests (93 total): context item renders as button not link, click calls refreshWorkOrderContext + refetches queue, shows "Refreshing…" during call.
- M18C-2 (complete): Kingdom Inbox UI Page. `InboxPage.tsx` at `/inbox` calls `GET /api/next-actions` and renders: (1) `TopActionCard` — highlighted card with priority score, risk badge, entity type badge, abstract state badge, escalated badge, title, why text, and action button linking to `routeTo`; (2) summary stats row (5 `StatCard`s: Pending, Critical, High, Blocked, Escalated); (3) ranked `QueueItem` list with the same badge set + priority score + age for each item. Empty state shown when `topAction === null`. Refresh button re-fetches live. Accessible to KING and CROWN_PRINCE (role-gated nav item in Command section with `Zap` icon; API enforces 403 for other roles). Added `NextActionItem`/`NextActionQueueDto`/`NextActionAbstractState`/`NextActionEntityType` types to `apps/web/src/types/api.ts`. Added `api.getNextActions()` to `apps/web/src/lib/api.ts`. 8 web tests (77 total, all pass): renders top action, renders queue, shows summary stats, empty state, refresh triggers re-fetch, top action links to routeTo, queue item links to routeTo, escalated badge present.
- M18C-1 (complete): Kingdom Next Action Engine — Backend Service + API. `nextActionService.ts` computes the highest-priority King action live across WorkOrders (NEEDS_REVIEW/FAILED/READY/IN_PROGRESS), AutomationJobs (QUEUED SANDBOX_PATCH/NEEDS_REVIEW/FAILED), PatchArtifacts (PENDING or HIGH/CRITICAL risk, not yet APPROVED), AgentRunners (OFFLINE/ERROR or stale heartbeat), HandoffBriefs (READY/IN_PROGRESS work orders with handoff brief but no post-handoff session), and AgentKnowledgeCandidates (batch count). Priority formula: `Math.min(100, baseScore + escalationBonus)` where `escalationBonus = Math.min(25, Math.floor(ageHours/12) * 4)` and `isEscalated = true` when bonus ≥ 16. Test data (`isTestData: true`) is excluded from all WorkOrder queries; AutomationJob and PatchArtifact items are excluded when their linked WorkOrder has `isTestData: true`. Returns `{ computedAt, topAction, queue, summary }`. API: `GET /api/next-actions` (KING/CROWN_PRINCE, query params: `limit`, `entityTypes` CSV, `minRisk`). Pure helper functions (`computeEscalationBonus`, `riskFromPriority`, `mapWorkOrderToActions`, `WorkOrderRow`) are exported for unit testing. No new Prisma migration — live computation only. Blocking chain bonus (`isBlocking`) deferred to M18C-3. 24 tests: 8 pure helper unit tests, 12 `mapWorkOrderToActions` unit tests (no DB), 4 integration tests (isTestData exclusion, topAction=queue[0], entityTypes subset invariant, minRisk subset invariant). Test baseline: 717 pass (693+24).
- M18A-4 (complete): Work Order UX Action Hardening. Three UX correctness fixes: (A) "Use This Agent" now immediately persists `assignedExternalAgentId` via a new focused API endpoint (`POST /api/work-orders/:id/assign-external-agent`, KING/CROWN_PRINCE) instead of only updating local React draft state; success shows "Assigned to <name>", error is surfaced inline, and the form select is updated synchronously from the new work order response. (B) STALE context messaging now correctly branches on `localDocsChanged`: when `current.binding.localDocsChanged === true`, the "Repair Context" button is suppressed (re-binding to the same stale snapshot is not a fix) and a clear explanation is shown: "Local docs changed since last scan — Bind/Refresh Context cannot fix this. Run a local docs scan on the linked project first, then re-bind context." The Repairable indicator is also set to NO in this case. (C) "Archive as Completed" action added — a King/Crown Prince-only button on the work order detail header that calls `POST /api/work-orders/:id/archive-completed` and sets `status = ARCHIVED`, `workQuality = COMPLETED_ARCHIVE`, `archiveReason = "Manually archived as completed by King"`, `archivedAt = now`; requires confirmation; archived WO disappears from the default active list; action is audit-logged. Used a focused endpoint to avoid the existing PATCH handler's `archiveReason = payload.constraints || ...` bug. 11 new API integration tests (assign-agent: 404, MINISTER 403, KING persists, Crown Prince can assign, audit log; archive-completed: 404, MINISTER 403, KING archives all fields, Crown Prince can archive, excluded from default list, audit log) and 12 new web tests (STALE message changes per localDocsChanged, Repair Context suppressed/shown, Use This Agent calls API + success + error + hidden from SCRIBE, Archive as Completed shown/hidden by role + calls API + confirms + not called on cancel + hidden when already ARCHIVED).
- M18A-3 (complete): Handoff Dedup Hardening. Fixed duplicate WorkOrder creation in the External Agent Handoff flow (`POST /tasks/:id/council/:sessionId/handoff`). Root causes: (1) handoff route used `sourceId: session.id` — every re-processed council session produced a different UUID, bypassing dedup and creating a new WO per session rather than per task; (2) when `createWorkOrder()` returned `EXISTING`, the route still called `createHandoffBrief()` unconditionally, accumulating redundant HandoffBriefs. Fix: changed `sourceType` to `"COUNCIL_HANDOFF"` and `sourceId` to `task.id` so dedup is keyed on the originating task (one handoff WO per task regardless of how many council sessions exist); session provenance is still captured in the `provenance` JSON field. Added EXISTING guard: when `createWorkOrder()` returns `EXISTING`, the route returns HTTP 200 with the existing WorkOrder and the latest existing HandoffBrief (no new brief created); a new creation path still returns HTTP 201 with a fresh brief. Frontend (ThroneRoomPage): shows amber "Context not fresh" banner after handoff creation when the returned WO has `contextBindingStatus` `STALE` or `MISSING`, prompting the King to run a local docs scan before creating SANDBOX_PATCH jobs. 3 new service-layer tests: sourceType/sourceId shape, repeated-handoff returns EXISTING same id, different-task same-title creates separate WO.
- M18A-2 (complete, hardened): WorkOrder Lifecycle Reconciliation. `workOrderLifecycleReconcileService.ts` detects active WorkOrders (READY/IN_PROGRESS/NEEDS_REVIEW, MISSING/STALE context) that are already completed by evidence and archives them, or repairs their context if no completion evidence exists. Evidence tiers: (1) `ImplementationReport` with `remainingWork = []` and `testResult = PASSED`; (2) `HandoffBrief` with completion keywords in `currentStatus` and no `nextSteps`; (3) `AutomationJob` with `status = COMPLETED` (non-validation); (4) `PROJECT_STATUS.md` word-boundary milestone code match with `(complete)` on the same line (future-proof for well-maintained status docs). Actions: ARCHIVED (evidence found, sets `archiveReason`, `archivedAt`, `workQuality = COMPLETED_ARCHIVE`); CONTEXT_REPAIRED (no evidence, project linked, snapshot available — delegates to `repairWorkOrderContext`); SKIPPED (no evidence, no project, or no snapshot). Hardening adds: NEEDS_REVIEW status in scope (frontend context-warning panel can surface any non-FRESH WO); `auditLog` entries written for every ARCHIVED action (`reconcile_archive_work_order`) and every context-repair attempt (`reconcile_context_repair_attempt`); `userId` threaded from the route through the options so audit entries attribute to the triggering user. API: `POST /api/work-orders/reconcile-context-warnings` (KING/CROWN_PRINCE). Frontend: "Reconcile Old Work Orders" button in Royal Brief Context Health section (shows result summary) and in Work Order context warning panel (bulk-reconciles and reloads list). 15 tests: 6 pure unit tests (`extractMilestoneCode`, `isMilestoneConfirmedInStatus`) and 9 DB integration tests covering each evidence tier, NEEDS_REVIEW archived, audit log created on archive, context-repair fallback, no-project SKIPPED, and unrelated WO exclusion. Safety: no runner execution, no automation job creation, no patch execution, no branch push, no PR creation, no merge or deploy.
- M18A-1 (complete): Context Rebind Repair. `repairWorkOrderContext(workOrderId)` and `repairProjectWorkOrderContexts(projectId)` added to `projectContextBindingService.ts`. Both wrap `bindFreshContextToWorkOrder` — skip when the work order has no linked project (`status: "SKIPPED"`, `skipReason: "no_project"`), attempt binding otherwise and return `previousStatus`/`newStatus`. Bulk repair queries all MISSING/STALE work orders for a project and repairs each. No scanner invocation — repair only re-reads whatever snapshot already exists. API: `POST /api/work-orders/:id/rebind-context` (KING/CROWN_PRINCE); `POST /api/projects/:id/rebind-contexts` (KING/CROWN_PRINCE). Work Order context panel now shows "Repairable: YES/NO" computed from the existing context binding response (`current.binding.localDocumentSnapshotId != null`); repairable panel includes an inline "Repair Context" button that calls the new endpoint. Royal Brief Context Health section now shows a "Repair Context" button per blocked work order (KING/CROWN_PRINCE only). 6 new API tests: missing→fresh, stale→fresh, no-project→skipped (row not mutated), snapshot-unavailable→remains-missing, bulk repair (MISSING+STALE both become FRESH, fresh WO untouched), and stale-snapshot bulk (repaired=0, attempted=1). Safety boundaries preserved: no runner execution, no automation job creation, no patch execution, no PR creation, no deploy.
- M18A (complete): External Agent Capability Router. `externalAgentRecommendationService.ts` scores all active `ExternalAgent` records against a work order's title, objective, context, and instructions using type-specific keyword signal tables (no LLM call, no DB writes). Returns a ranked `ExternalAgentRecommendation[]` with `score` (0–100), `confidence` (HIGH/MEDIUM/LOW), `reasons[]`, and `risks[]`. Type scoring matrix: CLAUDE_CODE (architecture, refactor, system design, codebase, migration, service layer), CODEX (bugfix, unit test, regression, fix failing, test coverage), CLINE (VS Code workflow, local file editing, command execution, terminal), ANTIGRAVITY (exploratory, browser, visual, prototype, screenshot, UI component), HERMES (handoff, coordination, status report, relay), KILO (multi-model, IDE/CLI support). Boilerplate fields (acceptanceCriteria, validationCommands) are excluded from the scoring corpus to prevent false inflation from standard templates. API: `GET /api/work-orders/:id/external-agent-recommendations` (any authenticated role, no requireRole). Frontend: Work Order detail page shows a "Suggested External Agent" card (between Project Context and External Prompt) with top recommendation, confidence badge, score, reasons, risks, "Use This Agent" button that pre-fills the agent dropdown, and a collapsible alternatives list. No auto-assignment, no auto-run — King manually selects and saves. 13 tests: 10 pure scoring unit tests + 3 DB integration tests (inactive excluded, NotFoundError, shape validation).
- M17I (complete, hardened): Council-to-WorkOrder Action Gate. After a council session completes, the Planner Agent can auto-create a reviewable work order gated by `COUNCIL_AUTO_WORK_ORDER_MODE = OFF | DRAFT | READY` (default OFF). `AUTO_PLAN_WORK_ORDERS` boolean is deprecated and removed. `runPlannerAgent()` reads the new setting and passes `targetStatus` (DRAFT or READY) to `createDraftWorkOrders()`, which now returns `draftedWorkOrderIds[]` for provenance tracking. The Grand Vizier orchestrator fires the planner after council completion (fire-and-forget, never blocks the council response). Manual trigger: `POST /api/council/:sessionId/plan-work-orders` and `POST /api/council/:sessionId/work-order` (KING/CROWN_PRINCE). Hardening (M17I-hardened): `createDraftWorkOrders()` now routes through `createWorkOrder()` from `externalAgentWorkOrderService` instead of calling `prisma.workOrder.create` directly — Data Value Gate, same-source duplicate detection, `createdBySystem` semantics, and context binding are all preserved. `PlannerResult` carries a `skipReason` field populated when `COUNCIL_AUTO_WORK_ORDER_MODE = OFF`. AI usage trace `triggerRoute` is now distinct for auto planner runs (`SYSTEM_ACTION: grandVizierOrchestrator (auto-planner)`) vs manual endpoint calls (the actual API route). Both routes return `skipReason` in their JSON responses. Repeated manual clicks on the same session are deduplicated by the cross-session title check in `hasDuplicateWorkOrder` plus the gate's own same-source duplicate REJECT rule. Gate behavior: DRAFT+SYSTEM_GENERATED → PREVIEW_ONLY (skipped without explicitUserAction; created with REVIEW_REQUIRED quality when triggered explicitly); READY+SYSTEM_GENERATED → PERSIST (always created). King remains the final execution gate — no auto-patch, auto-push, PR creation, or deployment is triggered.
- M17H (complete): Runner Result Agent Review. Added `AgentReviewSummary`, a one-review-per-automation-job advisory record generated after the runner submits an `ImplementationReport` and the API marks the job `NEEDS_REVIEW`. The runner still executes the job; a Kingdom Agent review layer reads structured, redacted runner output, patch artifacts, validation results, imported patch status, context status, risk, and blocked-path data, then writes a verdict, confidence, King recommendation, summary, passed/failed items, failed commands, risk notes, next actions, and an external-agent revision prompt when useful. Deterministic rules control verdicts and recommendations (`CHECK_FAILED` -> `PATCH_FAILED`/`RETRY_WITH_FIXED_PATCH`, `NO_CHANGES` -> `NO_CHANGES`/`REQUEST_REVISION`, `VALIDATION_FAILED` -> `VALIDATION_FAILED`/`REQUEST_REVISION`, low/medium-risk `VALIDATED` -> `PASS`/`APPROVE`, high/critical-risk `VALIDATED` -> `RISK_REVIEW`/`REVIEW_MANUALLY`); AI regeneration is best-effort JSON and falls back deterministically on provider errors or invalid output. API: `GET /api/automation-jobs/:id/agent-review`, `POST /api/automation-jobs/:id/agent-review/regenerate` (KING only, regenerate only for `NEEDS_REVIEW`). Web UI: `/automation-jobs` detail panel shows an Agent Review card with verdict/confidence/recommendation badges, summary, passed/failed/risk/action lists, failed commands, generate/regenerate button, and copyable external-agent prompt. Agent review does not approve patches automatically, does not push branches, does not create PRs, and does not merge or deploy; King remains the final approval gate.
- M17D: Living Loop Automated Kingdom Maintenance (M17D-1 through M17D-3, complete). `livingLoopService.ts` runs an observe -> propose -> act cycle (`runLivingLoopOnce`) gated by `LIVING_LOOP_ENABLED`, producing `AutomationCandidate` rows for work-order, validation, patch-review, memory, cleanup, provider, project, runner, and sandbox-patch observations, each passing `dataValueGate()` before persistence. `apps/runner` is a sandbox executor (`AgentRunner`/`AutomationJob`/`AutomationJobStep` models) that claims jobs over a token-authenticated API, runs allowlisted commands (`commandValidator.ts`), redacts secrets (`secretRedactor.ts`), and reports back via `ImplementationReport`. M17D-2 added opt-in `autoCreateValidationJobs()` (`LIVING_LOOP_AUTO_CREATE_VALIDATION_JOBS`) creating `VALIDATION_ONLY` jobs that only run read/typecheck/test/build commands — never edit files, never push. M17D-3 added `PatchArtifact` (risk-scored, blocked-path-checked, secret-redacted diffs with PENDING/APPROVED/REJECTED/REVISION_REQUESTED review states) and opt-in `autoCreateSandboxPatchJobs()` (`LIVING_LOOP_AUTO_SANDBOX_PATCH`), gated by `livingLoopRiskPolicyService.isAutoPatchEligible()` (daily limit, min confidence, riskLevel===LOW, online runner required, project-linked work order, no active/cooldown job, no blocked-path file hints). Auto-created sandbox patch jobs always carry `commandPolicy: "SANDBOX_PATCH_NO_PUSH"` and `provenance.source: "LIVING_LOOP_AUTO_SANDBOX_PATCH"` (with `loopRunId`/`candidateId`/`workOrderId`); the runner's `evaluateBranchPushEligibility()` blocks branch push for this policy regardless of `LIVING_LOOP_ALLOW_BRANCH_PUSH`, and `shouldPushWithoutApproval()` only allows unattended push for LOW-risk PENDING patches. No auto branch push, PR creation, merge, or deploy occurs — every auto sandbox patch lands as a `NEEDS_REVIEW` job with a pending `PatchArtifact` for King review. Dashboard and `/living-loop` page surface `autoValidation`/`autoSandboxPatch` status and `patchesPendingReview`; `/automation-jobs` tags auto-created jobs with provenance badges and a no-push notice; the Patch Review panel warns that auto-generated patches require King review.

## Current API Surface

- Public health: `GET /health`, `GET /health/db`.
- Auth: `POST /api/auth/login`, `POST /api/auth/logout`, `POST /api/auth/refresh`, `GET /api/auth/me`.
- Core command and knowledge resources: `/api/tasks`, `/api/council`, `/api/reports`, `/api/memory` (also `/api/memories`), `/api/agent-activities`, `/api/agent-conversations`, `/api/living-agents`, `/api/knowledge-candidates`, and `/api/knowledge-memories`.
- Admin/provider resources (KING only unless noted): `/api/agents`, `/api/settings`, `/api/providers`, `/api/provider-balances`, `/api/model-pricing`, `/api/route-chains`, and `/api/users`; external agent create/update/delete is KING-only.
- External work resources: `/api/external-agents`, `/api/work-orders`, `/api/work-sessions`, `/api/implementation-reports`, `/api/handoff-briefs`. Reads are authenticated. Work-order create/update and handoff generation are KING/CROWN_PRINCE. Implementation reports can be submitted by KING/CROWN_PRINCE/MINISTER. SCRIBE is read-only. `GET /api/work-orders/:id/external-agent-recommendations` (any authenticated role) returns ranked agent recommendations for the work order. `POST /api/work-orders/:id/rebind-context` (KING/CROWN_PRINCE) repairs a stale or missing context binding. `POST /api/projects/:id/rebind-contexts` (KING/CROWN_PRINCE) bulk-repairs all MISSING/STALE work order bindings for a project. `POST /api/work-orders/reconcile-context-warnings` (KING/CROWN_PRINCE) bulk-reconciles all READY/IN_PROGRESS/NEEDS_REVIEW work orders with MISSING/STALE context — archives those with completion evidence (with audit trail), attempts context repair on the rest (also audited), and returns a summary.
- Project workspace resources: `/api/projects`, `/api/project-routing`, `/api/project-inbox`, `/api/artifacts`, and `/api/projects/:id/export/obsidian`. Project reads are authenticated; create/update is KING/CROWN_PRINCE; delete/archive is KING-only. Project Inbox assignment is KING/CROWN_PRINCE. Artifacts can be created by KING/CROWN_PRINCE/MINISTER; SCRIBE is read-only.
- Treasury resources (KING only): `/api/treasury/overview`, `/api/treasury/usage`, `/api/treasury/agents`, `/api/treasury/providers`, `/api/treasury/reports`.
- Audit resources (KING only): `GET /api/audit`, `GET /api/audit/:id`, `GET /api/audit/search?q=`. Filters: `action`, `resourceType`, `userId`, `startDate`, `endDate`. Pagination: `page`, `limit`.
- Kingdom Charter and Vision (all authenticated for reads, KING for writes): `GET /api/charter`, `PATCH /api/charter`, `GET /api/vision`, `PATCH /api/vision`.
- Royal Secretary: `GET /api/secretary/brief` (all authenticated).
- Notices (read: all; create/delete: KING; update: KING+CROWN_PRINCE): `GET /api/notices`, `GET /api/notices/:id`, `POST /api/notices`, `PATCH /api/notices/:id`, `DELETE /api/notices/:id`.
- Matters (same RBAC as notices): `GET /api/matters`, `GET /api/matters/:id`, `POST /api/matters`, `PATCH /api/matters/:id`, `DELETE /api/matters/:id`.
- Living Loop (KING/CROWN_PRINCE read, KING write): `GET /api/living-loop/status`, `GET /api/living-loop/scheduler`, `GET /api/living-loop/runs`, `POST /api/living-loop/run`. Status includes auto-validation, auto-sandbox-patch, and M21 Auto Context Repair counters/gates.
- Automation Candidates (KING/CROWN_PRINCE read, KING write): `GET /api/automation-candidates`, `POST /api/automation-candidates/:id/approve`, `/reject`, `/archive`, `/apply`.
- Automation Jobs (KING only): `GET /api/automation-jobs`, `GET /api/automation-jobs/:id`, `POST /api/automation-jobs`, `POST /api/automation-jobs/:id/approve`, `POST /api/automation-jobs/:id/cancel`, `GET /api/automation-jobs/:id/agent-review`, `POST /api/automation-jobs/:id/agent-review/regenerate`.
- Patch Artifacts (read: authenticated; approve/reject/request-revision/create-pr: KING only): `GET /api/patch-artifacts`, `GET /api/patch-artifacts/:id`, `POST /api/patch-artifacts/:id/approve`, `/reject`, `/request-revision`, `/create-pr`.
- Runner: `/api/runners` provides King-governed runner records; token-authenticated `/api/runner` endpoints handle heartbeat, job claim, step recording, status updates, patch artifact submission, and report submission outside the user-facing RBAC surface.
- Kingdom Operations Center (KING/CROWN_PRINCE): `GET /api/kingdom/presence` (agent state machine across all agents), `GET /api/kingdom/activity?limit=N` (unified activity stream, last 48h), `GET /api/kingdom/health` (6 health dimensions with HEALTHY/WARNING/CRITICAL status).
- Kingdom Next Action Engine (KING/CROWN_PRINCE): `GET /api/next-actions` — live-computed priority queue of the highest-priority next King action across all entity types. Query params: `limit` (default 20, max 100), `entityTypes` (comma-separated filter: WorkOrder, AutomationJob, PatchArtifact, AgentRunner, HandoffBrief, AgentKnowledgeCandidate), `minRisk` (LOW/MEDIUM/HIGH/CRITICAL). Returns `{ computedAt, topAction, queue, summary: { totalPending, criticalCount, highCount, blockedCount, escalatedCount } }`.
- Mission Control and decision support: `GET /api/mission-control` (KING/CROWN_PRINCE), `/api/kingdom/presence|activity|health`, `/api/royal-brief`, and `/api/strategy`.
- Usage provenance: `/api/usage-traces` exposes trace/timeline evidence for authenticated users; runner endpoints remain token-authenticated and separate from user RBAC.

RBAC is enforced server-side per route and HTTP method. `KING` owns administration and execution approvals; `CROWN_PRINCE` has operational/project/work-order access; `MINISTER` has permitted command/report/project workflows; `SCRIBE` is read-only on permitted records. Frontend navigation mirrors these rules but is not the security boundary.

## Current Web Routes

Implemented pages: `/login`, `/dashboard`, `/inbox`, `/kingdom/operations`, `/royal-brief`, `/living-loop`, `/throne-room`, `/council`, `/strategy`, `/projects`, `/projects/:id`, `/work-orders`, `/project-inbox`, `/artifacts`, `/reports`, `/memory`, `/agent-chat`, `/knowledge-lab`, `/knowledge-lab/candidates`, `/knowledge-lab/memories`, `/charter`, `/vision`, `/living-agents`, `/living-agents/:agentId`, `/agents`, `/external-agents`, `/providers`, `/routing`, `/automation-jobs`, `/decree-lineage/:workOrderId`, `/treasury`, `/usage-traces/:traceId`, `/audit`, `/settings`, `/users`, `/notices`, `/matters`, `/security`, and `/profile`.

Navigation is role-aware and grouped into Mission Control / Command / Work / Knowledge / Agents & Models / System. Mission Control is read-only and source-linked; lifecycle actions remain on owning pages. The frontend handles access-token refresh and clears the session when refresh fails.

## Planned UX/UI Refinement

The current 39-route frontend has a page-by-page visual refinement roadmap in `docs/UX_UI_REFINEMENT_PLAN.md`. Wave 1 shared shell/layout work and all five Mission Control surfaces are complete. Wave 2 is complete across Throne Room, Council, Work Orders, Automation Jobs, Reports, and Decree Lineage. Wave 3A is complete across Projects Portfolio and Project Detail. These surfaces use semantic English/Thai chrome and preserve existing APIs, source ownership, context rules, and execution gates.

Wave 1, the full command-to-execution Wave 2, Wave 3 Projects/Strategy, and Waves 4A-4E are complete. The next refinement candidate is Wave 4F: Providers Registry. It starts with a source-ownership and secret-safety audit before any layout change. Carousels remain optional and must never hide required decisions, queues, reports, audit evidence, or source links.

## Verification

Latest frontend validation after Premium UX Wave 4E (2026-06-24):

- `npm run test --workspace @ai-kingdom/web` — 219/219 pass across 31 files
- `npm run typecheck` — pass across API, runner, and web
- `npm run lint --workspace @ai-kingdom/web` — pass
- `npm run build` — pass across API, runner, and web (pre-existing >500 kB chunk warning only)
- Focused Living Agent profile coverage — 8/8 pass
- Responsive browser verification is outstanding because the available in-app browser tab was locked on an error document during this run.

Latest full frontend validation after Premium UX Wave 4D (2026-06-23):

- `npm run typecheck` — pass across API, runner, and web
- `npm run test --workspace @ai-kingdom/web` — 211/211 pass
- `npm run build` — pass across API, runner, and web (pre-existing >500 kB chunk warning only)
- Live Wave 4D verification covers `/living-agents` at 1440x900, 1024x768, and 430x932 in English and Thai. Desktop panes align; tablet/mobile use one explicit pane at a time; state/role filters, assignment fallback, source links, 44px controls, reduced-motion-safe feedback, and horizontal-overflow checks pass. Browser logs contain no errors and only the existing React Router v7 future-flag warnings.
- Live Wave 4B verification covers `/external-agents` at 1440x900, 1024x768, and 430x932 in English and Thai. Registry hierarchy, runner evidence, source links, five focused sections, explicit dialogs, 44px controls, background scroll locking, reduced-motion-aware mobile handoff, and horizontal-overflow checks pass. Browser logs contain only the existing React Router v7 future-flag warnings.
- Live Kingdom verification covers `/throne-room` at 1440x900 and 430x932 in English and Thai. Real-state labels, deterministic room-bounded travel, ambient-idle disclosure, canonical portraits, selected source detail, 44px mobile controls, and horizontal-overflow checks pass. Browser logs contain only the existing React Router v7 future-flag warnings.
- Live Wave 4A verification covers `/agents` at 1440x900, 1024x768, and 430x932 in English and Thai. Registry hierarchy, canonical/display identity ownership, source links, six focused sections, explicit dialogs, row validation, 44px controls, background scroll locking, reduced-motion-aware mobile handoff, and horizontal-overflow checks pass. Browser logs contain only the existing React Router v7 future-flag warnings.
- Live Wave 3C verification covers `/strategy` at 1440x900, 1024x768, and 430x932 in English and Thai. Strategic hierarchy, section navigation, source links, explicit create/edit dialogs, 44px controls, background scroll locking, and horizontal-overflow checks pass without observed console errors.
- Live Wave 3B verification covers `/project-inbox` and `/artifacts` at 1440x900, 1024x768, and 430x932 in English and Thai. Queue/archive hierarchy, routing evidence, source links, modal behavior, touch targets, reduced-motion coverage, and horizontal-overflow checks pass without observed console errors. The real artifact archive holds long implementation-report titles to two list lines while retaining the full title in detail and tooltip views.
- Live shell verification covers expanded/collapsed navigation at 1440x900 and the mobile header/drawer at 430x932. Dashboard horizontal overflow is eliminated (`scrollWidth` equals viewport width), long provenance labels remain bounded, and the icon-rail preference persists.
- Live EN/TH verification (preview server, KING login) covers all Mission Control pages, including `/kingdom/operations`, `/royal-brief`, and `/living-loop` at desktop, tablet, and mobile widths. Domain navigation, mobile drawer, responsive ordering, page hierarchy, metrics, source links, disclosures, translated enum labels, and Thai chrome render without observed overlap or clipping; server-provided prose remains unchanged data and out of scope.
- Live EN/TH verification for Wave 2A covers `/throne-room?view=command` and `/council` at 1440px, 1024px, 430px, and 200% zoom. Command ordering, mobile action placement, mode/evidence disclosures, archive composition, source links, and translated wrapping render without observed overlap or horizontal clipping.
- Live EN/TH verification for Wave 2B covers `/work-orders` at desktop and 430px mobile widths plus 200% zoom. Queue-first mobile composition, explicit creation, disclosures, translated form chrome, and stable controls render without observed overlap or horizontal clipping.
- Live EN/TH verification for Wave 2C covers `/automation-jobs` at desktop and mobile widths plus 200% zoom. Metrics, quick filters, queue selection, responsive detail handoff, source links, section navigation, approval boundaries, runner evidence, and patch review render without observed overlap or horizontal clipping.
- Live EN/TH verification for Wave 2D covers `/reports` and `/decree-lineage/:workOrderId` at 1440x900, 1024x768, and 430x932. Archive selection, report provenance, the seven-stage evidence order, source links, bounded disclosures, Thai wrapping, 44px touch targets, reduced-motion rules, and horizontal-overflow checks pass without observed console errors.
- Prior baseline at `b6ce344`: typecheck/lint/build pass; web 132/132; focused provenance coverage 26/26
- HTTP smoke: web 200, API health 200
- Root `npm run test` did not complete: the serial API run reached 273 passing tests, then stopped producing output in the existing provider/orchestrator segment and was terminated; runner and web phases were not reached by that root command.

**Gotcha:** new Prisma migrations must be deployed to the `ai_kingdom_test` database (`npm run test:db:prepare`, or `prisma migrate deploy` with the test `DATABASE_URL`) *before* running root `npm run test`; otherwise route tests fail with 500s caused by test-DB schema drift.

The web production build still emits the existing Rollup warning for a JavaScript chunk larger than 500 kB. No schema migration was part of M20/M21.

## Known Gaps

- Audit log search is full-text across `action`, `resourceType`, `resourceId`, and user email only; metadata content is not searched.
- Auth does not include SSO, OAuth, MFA, password reset, or email verification.
- AI orchestration does not include tool calling or web search. Background autonomy now exists via the M19 scheduler (`kingdomSchedulerService.ts`) driving the gated Living Loop; there are still no fully autonomous tool-using agents.
- Anthropic and Gemini now have real runtime clients (Anthropic via a native Messages API client in `anthropicProvider.ts`; Gemini via its OpenAI-compatible endpoint through `OpenAICompatibleProvider`). Both activate in the registry when their API key is present. Local/Ollama remains a registry stub with no runtime client yet.
- Memory search is keyword/tag based; no vector database is implemented.
- Project routing is deterministic keyword/alias/name/source-ancestry matching only; no vector database or autonomous background router is implemented.
- Staging is configured but production deployment and production observability are not yet complete.
- `estimatedCostLocal` mirrors `estimatedCostUSD` (USD only; no FX conversion).
- The Grand Vizier appears twice per session in the UsageRecord table (once as a specialist agent call, once for the final synthesis pass), so per-agent call counts reflect actual AI invocations, not council participation.
- The `Budget` model is schema-only; budget limits are stored as `Setting` keys (`DAILY_BUDGET_LIMIT_USD`, `MONTHLY_BUDGET_LIMIT_USD`). No write API for `Budget` records exists yet.
- Usage records only cover sessions processed after M10 was deployed; historical sessions have no cost data.
- `LIVING_LOOP_AUTO_SANDBOX_PATCH` and `LIVING_LOOP_AUTO_CREATE_VALIDATION_JOBS` default to disabled; auto-created `SANDBOX_PATCH` jobs always require an online runner and a project-linked work order, and never push branches, open PRs, merge, or deploy — every patch lands `NEEDS_REVIEW` for King review.
- **Runner job modes (current capability boundary):**
  - `VALIDATION_ONLY` — copies the workspace, runs allowlisted typecheck/test/build/lint commands, submits a validation report. Never edits files, never creates a patch artifact, never runs `git add`/`commit`/`push`.
  - `SANDBOX_PATCH` — runs the plan's steps and validation commands, then diffs the workspace. If the workspace diff is empty (no files changed, no unified diff output) the runner submits a `NO_CHANGES` implementation report and **does not create a PatchArtifact**. If files were changed the runner submits a `PatchArtifact` (risk-scored, blocked-path-checked, secret-redacted) and lands the job `NEEDS_REVIEW`. The runner can now make real edits when a job carries `provenance.agentCli` (it drives a configured external agent CLI headlessly via `agentCliRunner.ts`); otherwise `FILE_CHANGE` plan steps are recorded as steps only. Safe patch-apply flow: the King can push an **already-approved** PatchArtifact to a safe `kingdom/job-*` branch via `POST /api/patch-artifacts/:id/push-branch`, which queues a SANDBOX_PATCH job with the approved diff as an imported patch and `commandPolicy: "APPLY_APPROVED_PATCH_PUSH"`. The runner re-applies it and pushes without a second approval — but only when the runner's `ALLOW_BRANCH_PUSH` setting is on, only to a safe branch (`pushSafeBranch` SAFE_PATTERN), and never merges or deploys. PR creation remains a separate explicit King action (`/create-pr`) that requires the branch to have been pushed.
  - **Imported patch statuses** (M17G.5): When a King-supplied unified diff is attached to a `SANDBOX_PATCH` job via `POST /api/automation-jobs/:id/import-patch`, the runner tracks the patch lifecycle via `importedPatchStatus`: `PENDING` (stored, not yet applied), `CHECK_FAILED` (git apply --check failed; no artifact created), `APPLIED_IN_SANDBOX` (applied, validation in progress), `VALIDATED` (applied + all validation commands passed), `VALIDATION_FAILED` (applied + one or more validation commands failed; artifact is still created and lands `NEEDS_REVIEW` for King review), `NO_CHANGES` (applied but produced no file diff; no artifact created). **Patch import must happen before approval**: importing on a non-QUEUED job returns HTTP 409 Conflict. Branch push and PR creation remain disabled.
  - **Runner result reviews** (M17H): after report submission, the API creates/updates an `AgentReviewSummary` for the King. This is advisory only: it summarizes runner output and recommends approve/reject/revision/manual review, but it never approves a `PatchArtifact`, runs commands, applies patches, pushes branches, opens PRs, merges, or deploys.

## Known non-blocking:
- Vitest 4.x with Vite 5.x emits esbuild/oxc deprecation warnings; the web suite still passes.
- The serial root API suite can stall in the provider/orchestrator segment after substantial passing coverage. Isolate external-provider timing/config from the test environment before treating root `npm run test` as a reliable completion signal.
- The web bundle has a >500 kB chunk warning; route-level code splitting remains a performance follow-up.
